# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'weather issy' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for weather issy
  pod 'Kingfisher', '~> 7.0'
  pod 'RxSwift', '6.5.0'
  pod 'RxCocoa', '6.5.0'


  target 'weather issyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'weather issyUITests' do
    # Pods for testing
  end

end
